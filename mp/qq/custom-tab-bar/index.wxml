<view class="tabbar-wrap">
  <view 
	qq:for="{{tabbarList}}" 
	qq:key="index" class="{{item.text===activeTab?'active item':'item'}}" 
	bindtap="onTabbarTap"
	data-url="{{item.url}}"
	data-text="{{item.text}}"
  >
  {{index}}:{{item.text}}
  </view>
</view>