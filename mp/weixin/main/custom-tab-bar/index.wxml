<view class="tabbar-wrap">
  <view 
	wx:for="{{tabbarList}}" 
	wx:key="index" class="{{item.text===activeTab?'active item':'item'}}" 
	bindtap="onTabbarTap"
	data-url="{{item.url}}"
	data-text="{{item.text}}"
  >
  {{index}}:{{item.text}}:{{activeTab}}
  </view>
</view>