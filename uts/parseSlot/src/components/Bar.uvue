<template>
    <view>
        <text>this is component Bar</text>
        <text>{{title}}</text>
        <slot age="default age"></slot>
        <text>msg: {{msg}}</text>
        <text>desc: {{desc}}</text>
        <slot name="header" msg="msg from component Bar"></slot>
    </view>
</template>

<script lang="ts">
import { SlotsType } from 'vue'
export default {
  slots: Object as SlotsType<{
    default: { foo: string; bar: number }
    item: { data: number }
  }>,
  props: {
    msg: {
      type: String,
      default: "default msg"
    },
    desc: {
      type: String,
      default: "default desc"
    }
  },
  data(){
    return {
      title: "Bar title"
    }
  }
}
</script>